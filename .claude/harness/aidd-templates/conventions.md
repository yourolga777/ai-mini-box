# Conventions

> AIDD layer 3 — the operating manual. How code, docs, and process work
> in this repo. AI agents read this BEFORE writing code; tighten it
> whenever a defect could have been caught by a stricter convention.

## Code

- Language(s) and version pins:
- Lint / format tools:
- Testing framework:
- Max file size / cyclomatic limits (if enforced):

## Naming

- Files:
- Functions / methods:
- Branches:
- Commits (message format):

## Process

- How tasks are tracked:
- How work gets reviewed:
- How releases are cut:
- What requires human approval (anything an agent should NOT do unilaterally):

## Boundaries

- Areas of the codebase that are *off-limits* to automated edits:
- External services that must never be called from tests:
- Secrets handling:
